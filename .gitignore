# files
.vscode
.env
.env.local
yarn.lock
app.yaml

# folders
build
node_modules

storage.sqlite
token
# Bind-mounted SQLite (host ↔ docker share). Keep the dir, ignore
# the DB itself + sqlite WAL/SHM sidecars + manual .bak snapshots.
data/database.sqlite
data/database.sqlite-*
data/*.bak
